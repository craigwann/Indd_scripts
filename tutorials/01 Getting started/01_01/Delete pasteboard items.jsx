// DeleteAllPasteboardItems.jsx//// Modified 2010-12-13#target indesign// Check to see whether any InDesign documents are open.// If no documents are open, display an error message.if (app.documents.length > 0) {	var myDoc = app.activeDocument;		// Delete all objects on the pasteboard	var myObjectArray = myDoc.pageItems.everyItem().getElements();	for (var j = myObjectArray.length - 1; j >= 0; j--) {		var myItem = myObjectArray[j];		if  (myItem.parentPage == null) {			myItem.locked = false;			myItem.remove();		}	}}else {	// No documents are open, so display an error message.	alert("No InDesign documents are open. Please open a document and try again.")}
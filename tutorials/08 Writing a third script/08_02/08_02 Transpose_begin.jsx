//DESCRIPTION:Transpose the 2 characters on either side of the text cursor// Transpose.jsx//// Modified 2015-08-12// Keith Gilbert, Gilbert Consulting// http://www.gilbertconsulting.com//// The text cursor must be located between two adjacent text charactersMain();// If you want the script to be un-doable, comment out the line above, and remove the comment from the line below// app.doScript(Main, undefined, undefined, UndoModes.ENTIRE_SCRIPT,"Run Script");function Main() {	// Check to see whether any InDesign documents are open.	// If no documents are open, display an error message.	if (app.documents.length > 0) {		var myDoc = app.activeDocument;		// Do something here			}	else {		// No documents are open, so display an error message.		alert("No InDesign documents are open. Please open a document and try again.");	}}